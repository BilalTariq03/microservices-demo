apiVersion: v1
kind: Service
metadata:
  name: cartservice
  namespace: boutique
spec:
  type: ClusterIP             # ClusterIP = only accessible INSIDE the cluster
  selector:
    app: cartservice
  ports:
    - port: 7070
      targetPort: 8080
